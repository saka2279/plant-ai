# Changelog

このファイルにはPlant AIの主な変更内容を記録します。

## [0.3.1] - 2026-08-03

Plant AI v0.3.1では、保存済みの実測記録を削除して作り直さず、既存フォームで内容を修正できるようにしました。

### Added

- 各実測記録の「編集」から、測定日時、`raw_avg`、`raw_min`、`raw_max`、水やり、土の感触、メモを既存フォームへ読み込む再編集機能
- 保存データを変更せず新規登録モードへ戻る「編集をキャンセル」操作
- 編集中の記録について、対象の測定日時と`raw_avg`、変更保存用の見出しとボタンを表示するUI

### Changed

- 再編集時は元の記録を同じIDのまま直接更新し、新しい記録を重複作成しない処理を追加
- 測定日時を変更した場合に、保存済み記録の一覧を再整列し、Canvas時系列グラフを再描画

### Safety

- localStorageのキーと保存オブジェクト形式、実測最大500件、CSV入出力形式を維持
- v0.2.1以前を含む旧形式記録を引き続き読み込み、更新時も既存の記録IDと記録バージョンを維持
- ADC生値の0〜1023と`raw_min ≦ raw_avg ≦ raw_max`の検証を維持し、水分率として扱わない

## [0.3.0] - 2026-08-01

Plant AI v0.3.0として、Arduinoの測定結果を貼り付け、読み取ったADC生値と人間の観察を同じ実測記録として保存する操作フローと、「一鉢の観察台帳」を基調とした画面へ更新しました。

### Added

- Arduinoシリアル出力を貼り付けて`raw_avg`、`raw_min`、`raw_max`を抽出する機能
- 複数行のシリアル出力から、最新の完全な測定行を採用する処理
- 不完全な測定行に対して、不足している項目を具体的に表示するフィードバック

### Changed

- 測定結果を入力し、読み取った内容を確認して、実測記録として保存する3段階の操作フローへ整理
- 測定日時へブラウザの現在時刻を初期入力し、保存前に利用者が修正できるよう変更
- 水やり、土の感触、メモ、手動raw入力を、必要な場合だけ開く詳細項目へ整理
- 判断デモを初期状態では閉じ、実測記録の操作と区別
- ADC生値と人間の観察を同じ時刻の記録として残す「一鉢の観察台帳」を基調にUIを再設計
- 1366×768でシリアル出力の貼り付けから実測記録の保存までスクロールせず操作できるようレイアウトを調整
- 360px幅で横スクロールが発生しないスマートフォンレイアウトへ調整

### Verified

- 実機UNO R3の出力から最新行`uptime_ms=11008,raw_avg=410.0,raw_min=409,raw_max=410,samples=20`を抽出し、2026-08-01 05:37の実測記録として保存して、一覧とグラフへ反映できることを確認
- `node tests/measurement-records.test.js`が成功
- 全JavaScriptファイルの`node --check`が成功
- `git diff --check`が成功
- ブラウザコンソールにエラー・警告がないことを確認

### Safety

- localStorageのキーと保存形式、CSV形式、既存データの読み込み、実測最大500件、デモ最大30件、既存の判断デモ機能との互換性を維持
- CSV互換用ファイル名`plant-ai-actual-measurements-v0.2.1.csv`を維持
- ADC生値を水分率として扱わず、植物状態や水やりを科学的に確定した表現へ変更しない

## [0.2.1] - 2026-07-21

Plant AI v0.2.1として、ELEGOO UNO R3で取得したカポックの土壌水分ADC生値を、デモデータと分けて記録・可視化できる画面を追加しました。

### Added

- 測定日時、`raw_avg`、`raw_min`、`raw_max`、水やり有無、土の感触、メモの登録
- 実測記録専用のlocalStorage保存と新しい順の一覧表示
- `raw_avg`を古い順に描画する依存なしのCanvas時系列グラフ
- 水やりありの測定を橙色のひし形で区別する表示
- 実測記録専用CSVの書き出し・読み込み
- CSVの引用符、カンマ、改行、旧フィールド名、不正値を確認する依存なしテスト

### Changed

- デモ観察スナップショットと実測記録の表示・保存先・CSVを明確に分離
- 画面、README.md、AGENTS.mdの現在バージョンをv0.2.1へ更新
- README.mdへ実測記録方法、グラフの見方、CSVの扱い、GitHub Pages公開予定を追記

### Safety

- raw値が0〜1023のADC生値であり、水分率ではないことを画面へ明記
- 未入力の日時、土の感触、水やり情報を自動補完しない
- 不正CSVや読み取れない旧形式データを除外し、アプリ全体を継続動作

## [0.2.0] - 2026-07-10

Plant AI v0.2.0として、ELEGOO UNO R3と土壌水分センサー1本による実測準備を追加しました。Webアプリv0.1.1の既存機能は維持しています。

### Added

- ELEGOO UNO R3のA0生値をUSBシリアルへ約1秒間隔で出力するArduinoスケッチ
- 20回測定の平均値と、同じ測定内の最小・最大値を出力する処理
- センサー生値が水分率ではないことを示す起動メッセージ
- 使用部品、安全確認、配線、Arduino IDEでの書き込み手順をREADME.mdへ追加
- センサー未接続、空気中、乾いた土、湿った土の測定記録用`docs/calibration.md`

### Changed

- 将来の実機構成をESP32前提からELEGOO UNO R3／USBシリアル前提へ整理
- `DemoSensorAdapter`を維持し、将来`SerialSensorAdapter`を追加する構成をREADME.mdへ明記

### Verified

- Arduino AVR Boards 1.8.8のArduino Uno向けにスケッチのコンパイルが成功
- ELEGOO UNO R3をArduino UNO（COM6）として認識
- COM6へのスケッチ書き込みが成功
- センサー未接続のA0開放状態で65秒間、66行の連続シリアル出力を確認

### Pending hardware verification

- センサーモジュール型番と対応電圧の確認
- 各条件の実測と1分以上の連続測定

## [0.1.1] - 2026-07-10

Plant AI v0.1.1として、コンテスト説明用のデモ品質とデータの持ち出しやすさを改善しました。

### Added

- 各デモシナリオへ再現内容の短い説明を追加
- コンセプトデモと今後の個体別学習について画面へ明記
- 観察スナップショットの個別削除
- 観察スナップショットのCSV書き出し
- README.mdへ主要デモ4種類の動作確認結果を追加
- README.mdへGitHub Pages公開互換性を記載

### Changed

- 確からしさが予測精度ではなくセンサー情報の整合性であることを明確化
- 画面と文書のバージョンをv0.1.1へ更新

## [0.1.0] - 2026-07-10

Plant AI v0.1.0として、実機なしで審査用デモが成立する初期版を作成しました。

### Added

- カポック・デジタルツインのコンセプトデモ
- 表層水分、根元水分、鉢重量変化、温度、湿度の表示と手入力
- 表層・根元水分のスライダーと数値入力の同期
- 複数センサーの一致・一部一致・不一致判定
- 乾燥・水分あり・湿りすぎ・判断保留の状態表示
- 4段階の判断の確からしさ表示
- 初心者向けの複数の判断根拠
- 表面だけ乾燥、全体が乾燥、センサー不一致、水やり変化のデモシナリオ
- `DemoSensorAdapter`による実機交換可能なデータ取得構造
- 判定ロジックと画面表示の分離
- 観察スナップショットのlocalStorage保存
- 旧版履歴の読み替え処理
- 360px幅を考慮したスマートフォン向けレイアウト
- README.md、CHANGELOG.md、AGENTS.md
